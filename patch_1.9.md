### фича 1. 
Сделать логику добавления или не добавления долгов в общий счёт 
флоу 1: Юзер: дал в долг саше 200к. Бот: [карточка добавления долга "owe_me"] записал в долги, добавить транзакцию в общий счет? - 200.000 сум и кнопки да или нет; 
флоу 2: Юзер: Вернул долг Маше 500к (при том что не было сообщения ранее о том как он взял долг). Бот: [карточка добавления долга i_owe но сразу погашенная] Записал погашенный долг, добавить эти две транзакции в основной счет? + 500.000 сум, - 500.000 сум. да или нет.

### фича 2.
 добавить эндпоинт для выборочного удаления нескольких транзакций, и сделать кнопку такую в боте "удалить транзакции" -> дать ИИ контроль к этома ендпоинту что бы он мог уточнять какие транзакциии: по датам/по описанию или названию/"последние 10"  и удалить. 

## Фича 3: Стабильные расходы

---

### Сущность: `StableExpense`

| Поле | Тип | Описание |
|---|---|---|
| `id` | UUID | |
| `user_id` | FK | |
| `title` | string | Название (напр. "Телефон") |
| `amount` | decimal | Сумма |
| `category` | string | Определяется AI автоматически |
| `due_day` | int (1–31) | День месяца для оплаты |
| `is_paid` | bool | Оплачено в текущем месяце |
| `paid_at` | datetime? | Когда отмечено оплаченным |
| `is_active` | bool | Не удалён, но деактивирован |
| `created_at` | datetime | |

---

### Эндпоинты

| Метод | Путь | Описание |
|---|---|---|
| `GET` | `/stable-expenses` | Список всех стабильных расходов юзера |
| `POST` | `/stable-expenses` | Создать новый |
| `PATCH` | `/stable-expenses/:id` | Редактировать (сумма, название, дата) |
| `PATCH` | `/stable-expenses/:id/paid` | Отметить оплаченным |
| `DELETE` | `/stable-expenses/:id` | Удалить |
| `POST` | `/stable-expenses/ai` | AI-парсинг из текста → создание/редактирование |

---

### UI Flow (бот)

**Главное меню → кнопка "Стабильные расходы"**

```
[Стабильные расходы]
  ├── 📱 Телефон — 100 000 ✅
  ├── 🌐 Интернет — 250 000 ❌
  ├── + Добавить
  └── ← Назад
```

**Нажатие на расход → карточка:**
```
📱 Телефон
Сумма: 100 000 сум
Дата: каждое 5-е число
Статус: не оплачено

[✅ Отметить оплаченным]  [✏️ Редактировать сумму]
[🗑 Удалить]  [← Назад]
```

---

### Уведомления (Scheduler)

| Триггер | Сообщение |
|---|---|
| За 7 дней до `due_day` | "⏰ Через 7 дней: Телефон — 100 000 сум" |
| За 3 дня | "⚠️ Через 3 дня: Телефон — 100 000 сум" |
| В день `due_day` | "❗ Сегодня: Телефон — 100 000 сум. Ты оплатил?" → `[Да]` `[Нет]` |

**Ответ "Нет" на уведомление:**
```
→ "Убрать из стабильных расходов?"
   [Да — удалить]  [Нет]
        ↓                ↓
    DELETE /id     "Ты можешь отметить оплаченным 
                    в меню Стабильные расходы"
```

---

### AI Flow (`/stable-expenses/ai`)

**Input:** свободный текст от юзера
`"добавь стабильный расход — телефон, 100 тысяч, каждое 5-е"`

**AI делает:**
1. Извлекает: `title`, `amount`, `due_day`
2. Определяет `category` самостоятельно (напр. `"Связь"`)
3. Если не хватает данных — бот уточняет недостающее поле

**Карточка-подтверждение после парсинга:**
```
📋 Новый стабильный расход:

Название: Телефон
Категория: 📱 Связь
Сумма: 100 000 сум
Дата: каждое 5-е число

[✅ Сохранить]  [✏️ Изменить сумму]  [✏️ Изменить дату]  [❌ Отмена]
```

---

### Inputs / Outputs

**Input (создание):**
- `title` — обязательно
- `amount` — обязательно
- `due_day` — обязательно
- `category` — генерирует AI

**Output (карточка):**
- Название, категория, сумма, дата, статус оплаты
- Кнопки: редактировать сумму / отметить оплаченным / удалить
## Фича 4: Копилка

---

### Сущность: `SavingsJar`

| Поле | Тип | Описание |
|---|---|---|
| `id` | UUID | |
| `user_id` | FK | |
| `title` | string | Название (напр. "Машина") |
| `description` | string? | Опциональное описание |
| `photo_url` | string? | Опциональное фото |
| `target_amount` | decimal? | Целевая сумма (обязательно) |
| `current_amount` | decimal | Текущая накопленная сумма |
| `deadline` | date? | Срок (опционально) |
| `is_active` | bool | Не удалена |
| `created_at` | datetime | |

### Сущность: `SavingsTransaction`

| Поле | Тип | Описание |
|---|---|---|
| `id` | UUID | |
| `jar_id` | FK | |
| `amount` | decimal | Сумма пополнения |
| `note` | string? | Комментарий |
| `created_at` | datetime | |

---

### Эндпоинты

| Метод | Путь | Описание |
|---|---|---|
| `GET` | `/savings` | Список копилок юзера |
| `POST` | `/savings` | Создать копилку |
| `PATCH` | `/savings/:id` | Редактировать (название, фото, описание, срок) |
| `DELETE` | `/savings/:id` | Удалить копилку |
| `POST` | `/savings/:id/deposit` | Добавить деньги в копилку |
| `GET` | `/savings/:id/transactions` | История пополнений |
| `POST` | `/savings/ai` | AI-парсинг из текста → deposit или create |

---

### UI Flow (бот)

**Главное меню → кнопка "Копилки"**

```
[Копилки]
  ├── 🚗 Машина — 2 500 000 / 50 000 000
  ├── 🏖 Отпуск — 100 000 / 800 000
  ├── ➕ Добавить копилку
  └── ← Назад
```

**Нажатие на копилку → карточка:**
```
🚗 Машина

📷 [фото если есть]
📝 На новую тачку
💰 Накоплено: 2 500 000 сум
🎯 Цель: 50 000 000 сум
📅 Срок: 01.01.2026
📊 Прогресс: ████░░░░░░ 5%

[➕ Добавить деньги]  [✏️ Редактировать]
[🗑 Удалить]  [← Назад]
```

**Нажатие "Добавить деньги":**
```
→ "Сколько добавить в копилку 🚗 Машина?"
   [Юзер вводит сумму]
→ "✅ Добавлено 500 000 сум! Итого: 3 000 000 сум"
```

**Нажатие "Добавить копилку":**
```
→ "Введи название копилки"
→ "Добавь фото? (опционально)" [Пропустить]
→ "Добавь описание? (опционально)" [Пропустить]
→ "Укажи целевую сумму? (обязательно)
→ "Укажи срок? (опционально)" [Пропустить]
→ Карточка подтверждения + [✅ Сохранить] [❌ Отмена]
```

---

### AI Flow (`/savings/ai`)

**Сценарий 1 — пополнение:**
`"добавь в копилку машина 500к"`

```
AI распознаёт:
  action: "deposit"
  jar_title: "Машина"  →  матчит с существующей копилкой
  amount: 500 000

→ POST /savings/:id/deposit
→ "✅ Добавлено 500 000 сум в копилку 🚗 Машина! Итого: 3 000 000 сум"
```

**Сценарий 2 — создание:**
`"создай копилку на отпуск, хочу накопить 5 миллионов до лета"`

```
AI распознаёт:
  action: "create"
  title: "Отпуск"
  target_amount: 5 000 000
  deadline: ~01.06.2026

→ Карточка подтверждения
```

**Сценарий 3 — неясный запрос:**
`"добавь в копилку 200к"`

```
→ "В какую копилку добавить 200 000 сум?"
   [🚗 Машина]  [🏖 Отпуск]  [❌ Отмена]
```

---

### Inputs / Outputs

**Input (создание):**
- `title` — обязательно
- `photo` — опционально
- `description` — опционально
- `target_amount` — обязательно
- `deadline` — опционально

**Input (пополнение):**
- `jar_id` — обязательно
- `amount` — обязательно
- `note` — опционально
ВАЖНО, после добавления в копилку денег, надо спросить у юзера надо ли отнять эту сумму из основного счёта? логика похожа как в долги

**Output (карточка копилки):**
- Название, фото, описание, прогресс-бар, срок
- Кнопки: добавить деньги / редактировать / удалить


